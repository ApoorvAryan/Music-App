import { useEffect,useState } from 'react';import api from '../services/api';import SongCard from '../components/SongCard';
export default function Search(){const[q,setQ]=useState('');const[songs,setSongs]=useState([]);const[loading,setLoading]=useState(false);useEffect(()=>{const t=setTimeout(()=>{setLoading(true);api.get(`/songs?search=${encodeURIComponent(q)}`).then(({data})=>setSongs(data)).finally(()=>setLoading(false));},250);return()=>clearTimeout(t);},[q]);return <div><h1 className="text-3xl font-black">Search</h1><input className="input mt-5 max-w-2xl" autoFocus placeholder="Search by song, artist or album" value={q} onChange={e=>setQ(e.target.value)}/><p className="mt-3 text-sm text-slate-400">{loading?'Searching...':`${songs.length} result(s)`}</p><div className="mt-6 grid gap-3">{songs.map(song=><SongCard key={song._id} song={song} songs={songs} compact/>)}</div></div>}
