import { useEffect,useState } from 'react';import api from '../services/api';import Loader from '../components/Loader';import SongCard from '../components/SongCard';
export default function Favorites(){const[songs,setSongs]=useState([]);const[loading,setLoading]=useState(true);useEffect(()=>{api.get('/favorites').then(({data})=>setSongs(data)).finally(()=>setLoading(false));},[]);if(loading)return <Loader/>;return <div><h1 className="text-3xl font-black">Favorites</h1><div className="mt-6 grid gap-3">{songs.map(song=><SongCard key={song._id} song={song} songs={songs} compact/>)}{!songs.length&&<div className="glass rounded-2xl p-8 text-center text-slate-400">Your liked songs will appear here.</div>}</div></div>}
