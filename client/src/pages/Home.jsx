import { useEffect,useState } from 'react';import { motion } from 'framer-motion';import api from '../services/api';import Loader from '../components/Loader';import SongCard from '../components/SongCard';
export default function Home(){const[songs,setSongs]=useState([]);const[loading,setLoading]=useState(true);useEffect(()=>{api.get('/songs').then(({data})=>setSongs(data)).finally(()=>setLoading(false));},[]);if(loading)return <Loader/>;return <div><motion.section initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} className="mb-8 rounded-3xl bg-gradient-to-br from-orange-500 via-rose-500 to-slate-950 p-8 shadow-glow"><p className="font-bold uppercase tracking-widest text-slate-950/70">Tune Fox</p><h1 className="mt-2 max-w-3xl text-4xl font-black md:text-6xl">A dark, polished streaming app for your own catalog.</h1><p className="mt-4 max-w-2xl text-slate-100">Upload tracks, build playlists, search music, and keep favorites close with a persistent global player.</p></motion.section><div className="mb-4"><h2 className="text-2xl font-black">Fresh uploads</h2><p className="text-slate-400">Newest songs on Tune Fox</p></div>{songs.length?<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-5">{songs.map(song=><SongCard key={song._id} song={song} songs={songs} onDeleted={id=>setSongs(songs.filter(s=>s._id!==id))}/>)}</div>:<div className="glass rounded-2xl p-8 text-center text-slate-400">No songs yet. Login and upload your first track.</div>}</div>}
