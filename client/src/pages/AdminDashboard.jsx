import { useEffect,useState } from 'react';import toast from 'react-hot-toast';import api from '../services/api';import SongCard from '../components/SongCard';
export default function AdminDashboard(){const[stats,setStats]=useState({});const[users,setUsers]=useState([]);const[songs,setSongs]=useState([]);const load=()=>{api.get('/users/stats').then(({data})=>setStats(data));api.get('/users').then(({data})=>setUsers(data));api.get('/songs').then(({data})=>setSongs(data));};useEffect(load,[]);const removeUser=async id=>{if(!confirm('Delete user?'))return;await api.delete(`/users/${id}`);toast.success('User deleted');load();};return <div><h1 className="text-3xl font-black">Admin Dashboard</h1><div className="mt-6 grid gap-4 md:grid-cols-3">{[['Users',stats.users],['Songs',stats.songs],['Total plays',stats.plays]].map(([k,v])=><div key={k} className="glass rounded-2xl p-5"><p className="text-slate-400">{k}</p><p className="mt-2 text-3xl font-black text-fox">{v??0}</p></div>)}</div><section className="mt-8"><h2 className="text-2xl font-black">Manage Users</h2><div className="mt-4 overflow-hidden rounded-2xl border border-white/10"><table className="w-full text-left text-sm"><tbody>{users.map(u=><tr key={u._id} className="border-b border-white/10"><td className="p-4 font-bold">{u.name}</td><td className="p-4 text-slate-400">{u.email}</td><td className="p-4 capitalize text-fox">{u.role}</td><td className="p-4 text-right"><button onClick={()=>removeUser(u._id)} className="btn btn-ghost text-red-300">Delete</button></td></tr>)}</tbody></table></div></section><section className="mt-8"><h2 className="text-2xl font-black">Manage Songs</h2><div className="mt-4 grid gap-3">{songs.map(song=><SongCard key={song._id} song={song} songs={songs} compact onDeleted={load}/>)}</div></section></div>}
